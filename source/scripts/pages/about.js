import Typed from 'typed.js'

// Typed

var typed = new Typed('.js-typed', {
  stringsElement: '.js-typed-strings',
  typeSpeed: 50,
  backDelay: 1500,
  startDelay: 0,
  backSpeed: 25,
  loop: true,
});