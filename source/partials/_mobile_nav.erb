<nav class="c-mobile-nav js-mobile-nav">
  <button class="c-mobile-nav__close js-toggle-nav">
    <%= inline_svg("/icons/close.svg", class: "o-icon") %>
  </button>
  <div class="c-mobile-nav__section">
    <ul class="c-mobile-nav__list">
      <% data.menuElements.main_menu.each do |menuElement| %>
        <li class="c-mobile-nav__item">
          <a class="c-mobile-nav__link<%= (current_page.path.split('.')[0] == menuElement.href[1..-1] ? ' is-active"' : '') %>" href="<%= menuElement.href %>">
            <%= menuElement.title %>
          </a>
        </li>
      <% end %>
    </ul>
    <ul class="c-mobile-nav__list">
      <% data.menuElements.social_links.each do |menuElement| %>
        <li class="c-mobile-nav__item">
          <a class="c-mobile-nav__link c-mobile-nav__link--secondary" href="<%= menuElement.href %>">
            <%= menuElement.title %>
          </a>
        </li>
      <% end %>
    </ul>
  </div>
  <div class="c-mobile-nav__section">
    <a class="c-btn c-btn--primary c-btn--full" href="https://www.donorhut.com/secure/4078-0m1xdi" target="_blank">Donate now</a>
  </div>
</nav>