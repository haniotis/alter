---
transparent_nav: true

hero:
  heading: Venture Boldly.
  subheading: Scaling frontier ventures with Silicon Valley resources

stats:
  - digit: 5
    label: countries in transition
  - digit: 12
    label: ventures leading the way
  - digit: 2M +
    label: lives impacted by Alter portfolio companies
---

<div class="c-hero u-cover">
  <div class="o-container">
    <div class="c-hero__content">
      <h1 class="c-hero__heading"><%= current_page.data.hero.heading %></h1>
      <h3 class="c-hero__subheading"><%= current_page.data.hero.subheading %></h3>
      <div class="c-btn-group">
        <button class="js-trigger-video js-open-video c-btn c-btn--primary c-btn-group__item">
          <%= inline_svg("/icons/play-video.svg", class: "o-icon c-btn__icon") %>
          Watch Video
        </button>
        <a href="/about/" class="c-btn c-btn--contrast c-btn-group__item">About Us</a>
      </div>
    </div>
  </div>
  <div class="c-hero__meta">
    <div class="o-container c-hero__meta-container">
      <% current_page.data.stats.each do |stat| %>
        <div class="c-hero__meta-item">
          <h2 class="c-hero__meta-digit"><%= stat.digit %></h2>
          <p><%= stat.label %></p>
        </div>
      <% end %>
    </div>
  </div>
  <video playsinline autoplay muted loop poster="/images/poster.jpg" class="c-hero__video">
    <source src="/videos/alter.webm" type="video/webm">
    <source src="/videos/alter.mp4" type="video/mp4">
  </video>
</div>

<%= partial "partials/video" %>